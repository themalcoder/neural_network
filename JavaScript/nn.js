class NeuralNetwork {

    // This is the constructor function
    constructor(numberOfInputNodes, numberOfHiddenNodes, numberOfOutputsNodes) {
        this.input_nodes = numberOfInputNodes;
        this.hidden_nodes = numberOfHiddenNodes;
        this.outputs_nodes = numberOfOutputsNodes;
    }

    // Next step - Feed Forward Process
    feedForward(input) {
        let guess = 0;
        // Lots of Matrix Math
        return guess;   
    }

    // Working - we recieve inputs. The idea is we do weighted sum. 
}

